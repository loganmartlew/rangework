# Supabase migrations

Supabase SQL migrations and RLS policies live here.

Phase 2 adds the baseline auth/profile migration without locking the later
practice-session schema decisions that are still open in `baseline-plan.md`.
